{
  "name": "portfolio-webpage",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio site, with a unit test suite for its client-side scripts.",
  "main": "app.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "rm -rf .coverage-raw coverage && vitest run && node tests/tools/coverage-report.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "istanbul-lib-coverage": "^3.2.2",
    "istanbul-lib-instrument": "^6.0.3",
    "istanbul-lib-report": "^3.0.1",
    "istanbul-reports": "^3.2.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.7"
  }
}
